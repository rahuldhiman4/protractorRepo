export enum security {
    readAccess = "READ",
    witeAccess = "WRITE"
}

export enum operation {
    deleteAccess = "DELETE",
    addAccess = "ADD"
}

export enum type {
    user = "USER",
    group = "GROUP"
}